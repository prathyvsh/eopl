#lang scheme

; (lambda (a)
;  (lambda (a)
;    (a : 1 0)))

; In this expression, the labelled expression shows
; that the bound variable belongs to the first expression
; This would mean that the inner binding isn’t effective
; in its scope.
